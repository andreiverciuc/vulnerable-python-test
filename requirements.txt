Flask==1.0.2            # Vulnerable to several issues in older versions
Django==1.11.29         # Has multiple known vulnerabilities
requests==2.19.1        # Known vulnerability in older versions
PyYAML==3.13            # Vulnerable to arbitrary code execution
urllib3==1.23           # Multiple security issues in older versions
jinja2==2.10            # Template injection vulnerabilities
pillow==5.2.0           # Multiple security issues
sqlalchemy==1.2.17      # SQL injection vulnerabilities
werkzeug>=3.0.6 # not directly required, pinned by Snyk to avoid a vulnerability
